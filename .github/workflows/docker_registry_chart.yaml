name: lint stfc-cloud-docker-registry chart
on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - ".github/workflows/docker_registry_chart.yaml"
      - "charts/stfc-cloud-docker-registry/**"

jobs:
  helm-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.18.3
      - name: Lint Helm Chart
        # Note --strict=true != --strict with the former being stricter
        run: |
          cp charts/stfc-cloud-docker-registry/secret-values.yaml.template /tmp/secret-values.yaml
          helm dependency update charts/stfc-cloud-docker-registry/.
          helm lint charts/stfc-cloud-docker-registry --values charts/stfc-cloud-docker-registry/values.yaml --values /tmp/secret-values.yaml --strict=true
