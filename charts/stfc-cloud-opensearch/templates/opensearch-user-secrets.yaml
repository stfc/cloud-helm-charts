apiVersion: v1
kind: Secret
metadata:
  name: {{ (index .Values "opensearch-cluster").cluster.name }}-admin-credentials
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
    username: {{ .Values.adminCredentials.username }}
    password: {{ .Values.adminCredentials.password }}